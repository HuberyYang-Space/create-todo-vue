# 时间轴

create-todo-vue 每日实测流水：哪天做了什么、实测到了什么、踩了什么坑。

> 本文件随仓库走（2026-09-21 起 `.docs/` 已纳入版本管理，`package.json` 的 `files` 不含它，不会发进 npm 包）。按日期查历史用本文件；按条目 ID 查用 [archive.md](./archive.md)。
> 当前该做什么见 [roadmap.md](./roadmap.md)。

**状态图例**：⬜ 待办　🚧 进行中　✅ 已完成　⏸️ 已搁置　❓ 待决策　❌ 已放弃

---


| 日期 | 版本 / 事件 | 记录 |
|:--|:--|:--|
| 2026-09-21 | CTV-56 完成 + 文档维护 | **仓库迁到组织 `HuberyYang-Space` 后的元数据同步**。Hubery 迁的仓库，`gh api repos/Hub-yang/create-todo-vue` 现在返回新 `full_name`，即**旧路径只剩重定向**。同步 6 处（`package.json` 三处元数据、README 两个徽章含 shields 的 workflow status 路径、`src/constants` 的 `REPO_URL`）；**刻意不动两类**：README 署名指向个人账号 `Hub-yang`（账号仍在），以及全部 `Hub-yang/my-vue-dev-template` 引用（`gh api` 核实那个仓库没迁，`todo-scripts` 倒是也迁了）。**改 `REPO_URL` 走了一次红→绿**：先把 `banner.e2e.test.ts` 的字面量断言改成新 URL，跑出真红，再改常量、**重新 build**（E2E 跑的是 `dist/`）后转绿。`tests/banner.test.ts` 那三条 `REPO_URL` 断言刻意保留成常量引用——它们守的是「banner 第 4 行 = `(REPO_URL)`」这个排版结构，改 `banner.ts` 会红，不是恒等式。**同批文档维护**：两个 workflow 的注释全部中文化（`git diff` 核对过非注释改动只有 dispatch 的 `description` 与两个 step `name`，`run`/`uses`/`with` 一字未改，ci.yml 逻辑相对 `ec182e8` 完全没动，所以 CTV-55 已验证过的行为不受影响）；CLAUDE.md 补三条现役禁令（别拿掉 `--no-verify`、别把 `main` 加回 ci.yml 的 push、仓库 URL 别写回个人账号），151 → 161 行 |
| 2026-09-21 | CTV-54/55 完成 | **同环境重复检测去冗余**（`02d0c96`，dev 与 main 已齐平）。起因是 Hubery 问「todo-scripts 调过的发版去冗余，这边是不是也有」——有，而且比那边贵。**边界由 Hubery 划定：只砍同一环境内部的重复**，「本地一遍 + CI 一遍」是刻意的跨环境双保险，所以 todo-scripts 的 HB-38（pre-commit 瘦身成只跑 `eslint --fix`）**本仓库明确不做**，日常 `pre-commit` 保持全量 typecheck + lint:fix + test。**CTV-55 实测到的现场**：`cc4f8b0`（v1.10.0）在 Actions 上跑了**三轮**一字不差的完整门禁（CI on main 34455157644、Release on tag 34455162869、事后快进 dev 34543809892），日常提交稳定两轮（`4fa4519` / `b719f22` / `b11996e` 三组 run 的 SHA 逐一相同）。根因是 `ci.yml` 同时盯 `main` 和 `dev`，而两者之间一律快进（`git log --merges` **为空**，历史零 merge commit），同一个 SHA 被推两次。**`concurrency` 救不了**：`refs/heads/main`、`refs/heads/dev`、tag 三种 `github.ref` 分不进同一组。改法三处：push 收成只盯 `dev`、加 `concurrency` + `cancel-in-progress`、job 上加 `if` 跳过 `bumpp` 的发版提交。**两侧都见过真实现场**：普通提交 `02d0c96` → run 35571754225 **success，五步全跑**；临时探针分支上造的 `chore: release v9.9.9`（`c578c19`）→ run 35571902997 **skipped**，探针分支随后已删。推 `main` 后**没有新增 run**，对照 `4fa4519` 当初的两轮。**踩到一个坑**：`if` 表达式里 `'chore: release v'` 的 `: ` 会让 YAML 把整行当成嵌套映射，`pnpm lint` 直接报 parsing error，**整个表达式必须用双引号包起来**。**CTV-54**：`bumpp` 的版本号提交会触发 `pre-commit`，把 `build:prod` 十几秒前刚跑完的三项原样重跑（本机实测 2.03 + 4.88 + 1.15 ≈ **8.1s**），diff 只有一个版本号字段；`bumpp --help` 确认 `--verify` 默认 `true`，类型里有 `commit.noVerify`。**两条留白如实记账**：① `concurrency` 的取消行为没有实测，要连推两次才看得到；② `--no-verify` 的运行时效果只能在真发一次版时确认（`pre-commit` 的输出不再出现），本轮只验证了参数存在 |
| 2026-09-21 | CTV-49 完成 | **照搬 todo-scripts 的 TUI 字标**（`393f8c0`→rebase 后 `0adff0d`、`c124aae`、`7b9f6c5`，在 dev 之上）。新增 `src/banner.ts`：两个纯函数（挑渲染模式 / 排版那几行）+ 一层环境探测，依赖 figlet / gradient-string / terminal-link 三个只有它用得上的库。**两处设计由 Hubery 拍板**：字标用 `TODO-VUE` 而非 `CREATE-TODO-VUE`（实测后者 117 列，80/100 列终端一律退化成纯文本，渐变根本看不到）；框顶 intro 去掉版本号（字标尾行已写着 `v1.10.0 - HuberyYang`，同屏重复没意义），`plan.ts` 的 `buildIntroTitle()` 随之成为零消费方并删除。**CTV-45 的两条 E2E 守卫跟着版本号搬家而不是删除**。**字体裁成 8 字形子集**：完整 ANSI Shadow 102 个字形，产物 107.3 → 97.8 kB（gzip 36.8 → 35.7）；FIGfont 字形按位置排列删不得，用不到的换成空字形，代价是改品牌名会安静渲染成空白，故单测改为拿未裁剪字体的点阵逐字比对整幅字标。**17 个变异 0 存活**；单测 254 → 268、E2E 100 → 106，`build:prod` 退 0。**真实 pty 复验**：120 列渐变、60 列退化、`FORCE_HYPERLINK=1` 下 OSC 8 生效。**两条留白如实记账**：渐变着色与 OSC 8 分支在测试环境里量不出来，只在真实 pty 手验，没有自动化守卫。**流程上踩了两个坑**：① 在 worktree 里开工，而 `CLAUDE.md` 与 `.docs/` 都被 gitignore 排除、读不到，整件事做完才发现违反了「非交互三件套不可省 `--no-immediate`」，据此**把 `CLAUDE.md` 与整个 `.docs/` 都纳入版本管理**（两者都不在 `files` 里，不会发进 npm 包），并把「banner 的排列位置」「改 `BRAND_NAME` 要同步阈值与字体子集」两条契约写进 CLAUDE.md 正文；② 分支误从 `main` 切出而非 `dev`，缺 CTV-46/47/48 六个提交，已 rebase 到 `dev`（三个文件有交集，git 自动合并无冲突，合并后 `build:prod` 复跑退 0）；③ 变异脚本改 `src/` 而 E2E 跑 `dist/`，pty 复验时读到上一轮变异残留的产物，**变异验证之后必须重新 build** |
| 2026-09-21 | CTV-46/47/48 完成 | **模板阵容现代化**，六个提交（`c0162c0`..`85ba5c7`）在 dev 上待发版。**CTV-46**：六个内置模板 8 处依赖对齐官方 create-vite（`typescript ~5.9.3→~6.0.2` 跨大版本、`@vue/tsconfig ^0.8.1→^0.9.1`），`@types/node` 刻意只到 `^24.13.5` 不追 npm 最新的 26（主版本要跟 `engines` 的 Node 20/22 对齐）；真装真构建 12 条命令全退 0，`vue-tsc -b` 无 TS7016。**CTV-47**：删 `custom-vitesse` / `custom-vitesse-lite`（breaking）——full 停更 7 个月且 catalog 仍锁 vite 7，lite 锁 `pnpm@12.3.4` 导致 pnpm 10 用户 ENOEXEC 退 1，且官方 create-vite 本就不列 vitesse。`utils.test.ts` 的 fixture 改指 nuxi init，`npm exec` 五个包管理器分支覆盖不丢（实测 96→82 条，14 条缺口逐条对应）。**CTV-48**：新增 `pnpm sync:check` 漂移探测（联网、不进 CI）。终审查出入口判据 `file://${process.argv[1]}` 在含空格路径下**静默跳过 main、零输出退 0**（漂移报警器最坏的假绿），已改 `pathToFileURL`，并补 `MIRRORED_TEMPLATES` 与注册表的一致性钉子、`@types/node` 刻意偏离例外表、failed/drifted 分离。门禁 `build:prod` 退 0：单测 254 / E2E 100 |
| 2026-09-10 | **v1.10.0 发布** | ✅ npm 上线 1.10.0（`latest`），tag `v1.10.0` 与 GitHub Release 均已生成，Release workflow run 34455162869 success、main 上的 CI run 34455157644 success。含单笔改动 CTV-45（`19adedb`，`feat: show the CLI version in the intro line`），**Release notes 正常露出那一条**（feat 在 changelogithub 白名单内）。tag 解引用后指向 `cc4f8b0`，`git branch --contains` 确认**只在 `main` 上——连续第五次在 main 上发版**；发版后已把 `dev` 快进到 `main`，四个引用同步在 `cc4f8b0`。**产物已实测**（registry 下载的真实 tarball，不是本地构建）：119 文件、7 模板、7 个 `_gitignore`、0 个裸 `.gitignore`；真跑线上产物 → `┌  create-todo-vue v1.10.0`，强制开色时 dim 只包住 `v1.10.0`，`--version` 干净的 7 字节，`npx …@1.10.0 --version` 端到端返回 1.10.0，用户最初报错的那个组合（`vanilla-ts` + pnpm install）退 0。**顺带实测出一条对以后每次发版都成立的事实**：验收当天（09-11 07:50）`pnpm dlx …` 解析到的是 **v1.8.0**——不是 latest 也不是上一版，而是**最后一个发布满 24 小时的版本**。而 v1.8.0 恰好是模板还锁着 `vite ^7.3.1` 的那一版，所以那个时间窗里用 pnpm 的新用户装依赖仍然会失败。**推论：发版后 24 小时内不要用裸的 `pnpm create` 做验收，也不要据此判断线上是否正常** |
| 2026-09-10 | CTV-45 完成 | **起因是一次误诊，值得留档。** Hubery 报「Vanilla + TypeScript + pnpm 装依赖失败」，但线上 v1.9.0 的真实 tarball 交互式走完同一条路径**装成功、退 0**，复现不了。循证找到现场：`~/Library/Caches/pnpm/dlx/f2cbac65…` 的 **birth 时间是当天 15:14:05**（全新创建、不是旧缓存被 touch），里面装的却是 **v1.6.1**——那版模板还锁着 `vite ^7.3.1`，正是 CTV-42 修掉的坑。根因：**pnpm 11 默认启用 `minimumReleaseAge`（24 小时内发布的版本一律不取）**，而 v1.9.0 当时才发布 28 分钟，于是回退到刚满 24 小时的 v1.6.1。**实测对照**：`pnpm dlx …` → 1.6.1、`pnpm dlx …@latest` → **1.6.1（`@latest` 照样被拦）**、`npx -y …` → 1.9.0、`bun x` → 1.9.0；绕过方式只有两条——`@<精确版本>` 或 `--config.minimumReleaseAge=0`，两条都实测有效。用那份 v1.6.1 精确复现出 `└ 依赖安装失败`（`ERR_PNPM_IGNORED_BUILDS: esbuild@0.28.2`，退出码 1）。**CLI 代码与线上产物都没有缺陷**，真正的问题是**界面上看不出跑的是哪一版**，于是白测一轮无从察觉——据此开 CTV-45。改动只有两处：`plan.ts` 新增纯函数 `buildIntroTitle()`、`index.ts:142` 接线，`terminal.ts` 零改动。**先写红再实现**：单测 4 条红（`buildIntroTitle is not a function`）、E2E 2 条红（`expected 'create-todo-vue' to be 'create-todo-vue v1.9.0'`）。**这个改动打不红任何既有测试**（E2E 从没断言过 intro 行内容），所以新钉子全靠变异证明：**6 个变异 6 个被捕获**，还原后 sha256 与基线一致。未提交，等 Hubery 发话 |
| 2026-09-10 | CTV-28 放弃 | Hubery 决定**短期内不升 TypeScript**，最后一条待办条目标 ❌ 归档。按维护规则第 7 条**不删行**——这份文件不进版本库，删掉就没有 git 副本可找回，而条目里那条实测结论（`typescript-eslint@8.69.0` 的 peer 范围 `>=4.8.4 <6.1.0`）是「为什么 TS 压在 6.0.3」的唯一出处。**至此待办分组与候选池双双清空**，下一批做什么由 Hubery 提。零代码改动，只动 ROADMAP |
| 2026-09-10 | **v1.9.0 发布** | ✅ npm 上线 1.9.0（`latest`），tag `v1.9.0` 与 GitHub Release 均已生成，Release workflow run 34446734352 success、main 上的 CI run 34446729786 success。含单笔改动 CTV-42/43/44（一个 `fix:` 提交 `081c2ef`），**Release notes 正常露出那一条**（fix 在 changelogithub 白名单内）。tag 解引用后指向 `9008cb9`，`git branch --contains` 确认**只在 `main` 上——连续第四次在 main 上发版**；发版后已把 `dev` 快进到 `main`，四个引用同步在 `9008cb9`。**产物已实测**（registry 下载的真实 tarball，不是本地构建）：119 文件、7 个 `_gitignore`、0 个裸 `.gitignore`、0 `.npmrc`、7 份 README、vitesse 残留 0；**七个模板的 `vite` 全部是 `^8.2.2`**、`template-vue-ts` 两个 SFC 都带 `lang="ts"`、`vue-dev` 的 eslint 关规则那段与不含 `trustPolicy` 的 workspace 都发得出去、key 顺序为 `name → type → version → private`。**`npx @1.9.0` 实跑三个模板全退 0**，`.gitignore` 落地、git 已初始化；**对线上产物真跑 `pnpm install` + `pnpm build` 全退 0、esbuild 包数为 0，`vue-dev` 的 `pnpm lint` 也退 0**——三个缺陷在真实发布产物里确认修好。**留档一个查证坑**：核实 tag 指向时 `git rev-parse --short v1.9.0` 返回的是 **annotated tag 对象**的 SHA（`4c3a627`）而不是 commit，看着像「tag 不指向 main」，实则要用 `git rev-parse v1.9.0^{commit}` 解引用才得到 `9008cb9`。冷启动清单里那条「线上是什么版本 / tag 指向哪」以后按后者查 |
| 2026-09-10 | CTV-42 / CTV-43 / CTV-44 完成 | **一次全量验收测试查出三个模板缺陷并全部修掉。** 测试本身在 `~/Desktop/ctv-qa` 沙箱里做，分六层共 50+ 个真实用例（真 pty 交互 15 例 / 7 模板真装真构建真起 dev / 5 个 custom 真转交上游 / 真实 `-i` 联网安装 / 24 例边界矩阵 / 线上 tarball 实跑），**测完即删**。**CLI 自身零缺陷**——退出码透传、框线收尾、光标还原、非交互提示、git init 分支、`.git` 保留、6 个包管理器的指令改写，全部一次通过；**三个问题全在模板内容上**：① 六个模板锁 `vite ^7.3.1`，vite 7 的 `esbuild` 带 postinstall，pnpm 11 未放行即退 1，**线上 v1.8.0 同样中招**，用 pnpm 的用户选「立即安装依赖」直接看到 `└ 依赖安装失败`；② `template-vue-ts` 两个 SFC 缺 `lang="ts"`，vue-tsc 报 TS7016，**npm 用户一样构建不了**；③ `template-vue-dev` 开箱 `pnpm lint` 退 1（6 个错）。**没有一个能被现有 342 条自动化测试发现**。**修法**：六个模板升 `vite ^8.2.2`（vue / vue-ts 的 plugin-vue 同步升 `^6.0.8`）、vue-ts 补 `lang="ts"`、vue-dev 修 key 顺序与 `BaseFooter.vue` 折行、在模板 `eslint.config.ts` 里关掉 `pnpm/yaml-enforce-settings`。**中途翻过一次车**：先按字面把 eslint 要的三条 pnpm 设置补进 `pnpm-workspace.yaml`，lint 绿了但 `pnpm install` 反而退 1（`ERR_PNPM_TRUST_DOWNGRADE`，`semver@6.3.1` 经 vue-devtools 传递），**而那一刻五条静态断言全是绿的**——据此往 CLAUDE.md 加了第四条验证标准「模板改动必须真装一次、真构建一次，npm 与 pnpm 都要跑」。**验证**：`pnpm build:prod` 退 0，单测 244 → **249**、E2E 98 不变、`npm pack` 仍 119 文件；重测 7 模板 × (pnpm/npm) × (install/build) **28/28 退 0**、esbuild 包数全为 0、dev server **7/7 返回 200** 且 `<title>` 均为包名、真实 `-i` **4/4 退 0**、vue-dev 的 lint/tscheck/build 双包管理器全退 0、L1 交互回归 15/15。**12 个变异 12 个被捕获，0 存活**，还原后 diff 无残留。README 核对过无需同步。**已提交并推送**：`081c2ef`（单个 `fix:` 提交，会露在 Release notes 里），pre-commit 三件套全过；已 fast-forward 合进 `main`，四个引用同步在 `081c2ef`，CI run 34443881811（main）与 34443884526（dev）均 success。**已随 v1.9.0 发布**（minor，理由是 `vite` 主版本 7 → 8 属于用户可感知的行为变更，不是单纯 bugfix）。README 已核对无需同步 |
| 2026-09-09 | **v1.8.0 发布** | ✅ npm 上线 1.8.0（`latest`），tag `v1.8.0` 与 GitHub Release 均已生成，Release workflow 与 main 上的 CI 都 success。含 CTV-41（第七个内置模板 `vue-dev`）与一条无 ID 的 `fix: pin lint-staged to the root config`，两条都露在 Release notes 里（feat / fix 都在 changelogithub 白名单内）。**tag `v1.8.0` 指向 `1efd7dd`，`git branch --contains` 确认只在 `main` 上——连续第三次在 main 上发版**，发版后已把 `dev` 快进到 `main`。**产物已实测**（不是本地构建）：下载线上 tarball 共 119 文件，`template-vue-dev` 33/33 与仓库副本逐文件比对一致，`_gitignore` / `.husky/{commit-msg,pre-commit}` / `pnpm-workspace.yaml` / `.vscode/*` 全部发得出去，全包内不存在任何名为 `.gitignore` 或 `.npmrc` 的文件（符合 npm 的无条件剔除规则），LICENSE / lockfile / `.husky/_` 均未混入；`npx @huberyyang/create-todo-vue@1.8.0 live-app -t vue-dev` 真跑一次 → 33 文件、`name` 与 `<title>` 均改写成 `live-app`、README 仍是占位 `# vue-dev`、`.gitignore` 落地 257 字节、git 已初始化、身份字段零残留；**对线上产物再跑一次 `pnpm install` 退出码 0、无 `ERR_PNPM_IGNORED_BUILDS`**——本版核心修复在真实发布产物里确认生效 |
| 2026-09-09 | 提交时踩到 lint-staged 配置发现 | `fix: pin lint-staged to the root config`（`d60f55c`）。CTV-41 提交时**卡死 7 分钟、`kill -9` 杀不掉**。根因：`.husky/pre-commit` 是裸的 `npx lint-staged`，而 lint-staged 会**为每个暂存文件向上查找最近的配置**——`template-vue-dev/lint-staged.config.mjs`（内容 `{'*': 'eslint --fix'}`，是模板自身的一部分，不能删）于是接管了全部 33 个模板文件，用**本仓库的** eslint 去 `--fix` 它们，而模板的 `eslint.config.ts` 依赖本仓库没装的插件。修法是给 lint-staged 传 `--config lint-staged.config.mjs`，该参数会禁用配置发现。**这不是 CTV-41 特有的**：以后任何带 lint-staged / eslint 配置的新模板都吃这条保护。已记进 CLAUDE.md |
| 2026-09-09 | CTV-41 完成 | 新增第 7 个内置模板 `template-vue-dev`（上游 `Hub-yang/my-vue-dev-template`，33 文件）。**三个决策由 Hubery 拍板**：走内置而非 `custom-` 转交上游（代价是上游更新要手动同步）、名字取 `vue-dev`（去掉作者视角的 "my"、不与 `Official Vue Starter` 撞概念）、演示内容保留结构但精简正文（两段各 7523 字符的 Lorem ipsum → 一句短文本，`index.vue` 从 17069 → 2381 字节）；footer 里指向上游仓库的 GitHub 链接**按 Hubery 要求保留**作项目宣传。**剥离项**：`package.json` 的 author / homepage / repository / bugs / license / packageManager 全删，`LICENSE`、`pnpm-lock.yaml`、`.husky/_`（16 个 husky 生成的运行时）不拷。**两个待实测项都在实测中被推翻或确认**：① 不带 `pnpm-workspace.yaml` 时 `pnpm install` **实测退出码 1**（`ERR_PNPM_IGNORED_BUILDS: @parcel/watcher`），而 CTV-39 让 `-i` 透传退出码，用 pnpm 的用户选「立即安装依赖」会直接看到创建失败——故补了一份只含 `allowBuilds` 的精简版，补后 install 退 0；② `vite.config.ts` 里 `target: ''` 的 proxy 占位**实测无害**（dev server 正常启动、首页 200、`/api` 返回 502 且 server 仍存活），故保留。**验证**：`build:prod` 退出 0（typecheck / lint:fix / 244 单测 / build / 98 E2E），单测 241 → 244、E2E 96 → 98（新模板被 3 条既有 `it.each` 自动纳入），`npm pack` 从 86 → 119 文件且新模板 33/33 全部发得出去（`_gitignore`、`.husky/*`、`pnpm-workspace.yaml` 均在，LICENSE / lockfile / `.husky/_` 均未混入），真实 `pnpm install` + `pnpm build`（含 `vue-tsc --noEmit`）均退 0。**10 个变异 10 个被捕获**。**新增 3 条钉子**（都对所有模板生效，非仅 vue-dev）：不带上游身份字段、不锁 `packageManager`、`template-vue-dev` 必须带放行 `@parcel/watcher` 的 `pnpm-workspace.yaml` |
| 2026-09-09 | **v1.7.0 发布** | ✅ npm 上线 1.7.0（`latest`），tag `v1.7.0` 与 GitHub Release 均已生成，Release workflow run 34333464060 success、CI run 34333460252 success。含四笔改动：CTV-33（测试钉子）、CTV-24（删 renovate 配置）、CTV-22（自动 git init）、CTV-25（六份占位 README）。**在 `main` 上发的，连续第二次走对**；发版后已把 `dev` 快进到 `main`，四个引用同步在 `fa86d58`。**产物已实测**（registry 下载的真实 tarball）：**86 个文件**（v1.6.1 是 82，正好多出 4 份新增 README），六份 `template-*/README.md` 逐个点名核对全部就位，直接从 tarball 里读出内容确认是 `# lit` / `# vue-ts` 而非上游英文原文；`src/` / `tests/` / renovate 命中全为 0。**`npx @1.7.0` 实跑两条路径**：普通场景生成后 `git rev-parse --is-inside-work-tree` 为 true、分支 `main`、README 是 `# vue-ts`；在已有 git 仓库内则打印「目标目录已在 git 仓库中，跳过 git init」且子目录**没有** `.git`，退出码 0。`--version` 输出 1.7.0。**provenance 仍为空**——符合当天 CTV-23 的放弃决定，不是疏漏。**Release notes 露出 3 条**（2 个 feat + 1 个 docs），CTV-32 扩展的 docs 类型第二次生效 |
| 2026-09-09 | CTV-22 / CTV-25 完成 | 两条一起做。**两个条目的前提都被实测推翻**：CTV-22 写着「create-vue 做了」，而上游 `index.ts` 现在只提示不执行；CTV-25 写着「要维护 8 份 README」，实际只有 2 份、另外 4 个模板一份都没有。Hubery 据此拍板：git init 确定要做（只 init、已在仓库内则跳过）、README 六份统一成静态的模板名占位。**TDD 走完**：先写 4 条 git E2E 看到 3 红 1 绿（那 1 绿是反向钉子，实现后仍须绿），再实现；CTV-25 同样先写 constants 断言看红再补文件。**改了一处测试基础设施**：`fixture.tree()` 不再列 `.git`，加上排除后既有 91 条一条没变，是干净信号。**补了一个缺口**：新增 4 份 README 没打红任何既有 E2E（只有 vue-ts 有完整树断言），于是加了一条「README 不被改写成项目名」的钉子。**8 个变异 8 个被捕获**（不真建仓库 / findGitDir 不向上找 / 无条件跳过 / gitInit 不指定 cwd / tree 不排除 .git / README 内容改坏 / 删掉一份 README / scaffold 改写 README），还原后 6 个文件 sha256 与基线逐个比对全部一致。验证：`pnpm build:prod` 退出 0，单测 240 → **241**、E2E 91 → **96**。README 与 CLAUDE.md 已同步。**已提交并推送**：`d13384c`(feat git init) / `b57b1e5`(feat 占位 README) / `dd23080`(docs)，刻意拆成三个提交让每个自洽；四个引用同步在 `dd23080`，CI run 34332999577(main) 与 34333004368(dev) 均 success。**待发版** |
| 2026-09-09 | CTV-40 放弃 | Hubery 看过条目内容后决定**不加发版分支护栏脚本**，发版分支由他自己控制。讲解时顺带实测出一个原方案里的机制问题并留进条目：`prerelease` 是 `release` 的**自动** pre 钩子名，`pnpm run release`（pnpm@11.25.0）与 `npm run release`（npm 11.6.2）都会自动先跑它——原方案再显式串一次就会跑两次；改成只靠钩子则会依赖 `enable-pre-post-scripts`，被关掉时**静默失效**，恰是该条目当初警惕 `preversion` 时怕的形态。另指出「只判当前分支是不是 main」挡不住 `main` 落后于 `origin/main` 的情形。**零代码改动**，只动 ROADMAP |
| 2026-09-09 | CTV-24 处置：删掉 renovate 死配置 | 挑中 CTV-24 后，探索直接推翻了条目的前提。条目问的是「renovate 提的模板升级 PR 是保鲜还是噪音」，而实测发现**renovate 在这个仓库从来没跑过**：配置加于 2026-03-05，半年里 0 PR / 0 issue / 0 `renovate/` 分支 / 0 renovate 提交，`lockFileMaintenance` 本该每周一跑，schedule 窗口也到过（09-06 是周日），`.github/workflows/` 里也没有 self-hosted action——App 从未安装。**再跑官方 `renovate-config-validator`，配置本身还是坏的**：`Invalid schedule: Failed to parse "after 10pm every Sunday and before 5am every Monday"`，装了 App 也只会报配置错误。据此把问题从「怎么配」换成「要不要用」，**Hubery 决定不用**。改动：删 `.github/renovate.json5`（唯一进版本库的改动，`.github/` 只剩 `workflows/`）；改写 CLAUDE.md 里那句**本身就是错的**描述（它说 renovate「会扫到模板并提 PR」，而那件事从未发生）。**刻意不走 TDD**：删的是 CI 配置、`src/` 零改动、没有可断言的行为变化，硬写「断言文件不存在」是没牙齿的恒等式。验证：`grep -ri renovate` 在仓库里归零、`pnpm build:prod` 退出 0、`npm pack --dry-run` 仍 82 个文件 。**已提交 `8a1f225`（`ci:` 类型，不进 changelogithub 白名单，对使用者零影响）**，未推送 。**已推送，四个引用同步在 `8a1f225`**，CI run 34329797282（main）与 34329802122（dev）均 success |
| 2026-09-09 | CTV-23 放弃 | Hubery 挑中 CTV-23 后，核实发现**条目描述的前提已失效**：它写着「两个仓库可以一起迁」，而 todo-scripts 的 HB-30 早两天（2026-09-07）就已标 ❌ 放弃，那边 memory 里甚至留着「不要再把 OIDC 迁移当待办提出来」。把这个冲突连同「CTV-23 到底改什么」一起摆出来后——它改的不是「谁决定发版」（选版本号、推 tag、什么时候发全都还是本地、还是 Hubery），只是 `npm publish` 这条命令从本地挪进 CI——**Hubery 决定不做，发版维持现状**。代价（包继续没有 provenance）已知且接受，理由与 todo-scripts 一致：不动一套已经稳定跑通的流程。**本次查证的结论全部写进条目留档**，重开时不必重新调研；其中最有价值的一条是 `repository.url` 与 `GITHUB_REPOSITORY` 已字符串一致（todo-scripts 当年栽在这，本仓库天然通过）。**`src/` 与 workflow 零改动**，只动了 ROADMAP。开工前顺带跑了门禁基线：`pnpm build:prod` 退出 0（单测 + E2E 7 文件 91 条全绿），工作区干净，四个引用同步在 `b08a6f1` |
| 2026-09-09 | CTV-33 完成 | 审计「把模块级常量直接交给第三方库」的同类风险。**结论：`src/` 里没有第二处**——第三方库只有 mri / cross-spawn / @clack/prompts / picocolors 四个，`FRAMEWORKS` 交给 clack 的两处是 `.map()` 返回新对象，其余常量只流向自有纯函数（四个都逐行读过）。**但扫出了防护缺口并补上**：`ARGV_OPTIONS` 有钉子、`FRAMEWORKS` 与 `RENAME_FILES` 没有，实测注入污染后 239 条单测全绿一条没红（派生物 `TEMPLATES` / `HELP_MESSAGE` 都是加载时快照，挡不住）。新钉子的期望值全硬编码，**3 个变异 3 个被捕获**。**既有钉子也顺带验证了**：拿掉 `structuredClone` 后 `boolean` 从 4 项涨到 191 项，两条用例转红。**中途 typecheck 抓到一个真错**（`f.variants` 类型上可选，我用 `tsx` 取值时绕过了类型检查），修完**重新验证变异仍能捕获**才算数。`build:prod` 退出 0，单测 239 → 240、E2E 91 不变，`src/` 零改动。**已提交并推送**：`b08a6f1`，四个引用同步在该提交，CI run 34319246351（main）与 34319244763（dev）均 success。**未发版**——纯测试改动，攒着跟下一批一起发 |
| 2026-09-09 | **v1.6.1 发布** | ✅ npm 上线 1.6.1（`latest`），tag `v1.6.1` 与 GitHub Release 均已生成，Release workflow run 34307255525 success、CI run 34307249533 success。含 CTV-38 与 CTV-32 两条。**这次是在 `main` 上发的**——v1.5.1 / v1.6.0 连着两次误在 `dev` 上发，这次走对了；发版后已把 `dev` 快进到 `main`，四个引用同步在 `00b87d3`。**产物已实测**（registry 下载的真实 tarball）：82 个文件，新增的 `changelogithub.config.ts` 命中 0（确认不随包发出去），六个模板的 `package.json` 与 `_gitignore` 逐个核对就位，`src/` / `tests/` / e2e / vitesse / `.npmrc` 命中全为 0。**把 v1.6.0 与 v1.6.1 两个发布产物的 `dist/index.js` 直接比对，hash 完全相同（`64ef0932…`）——「零运行时影响」因此不是靠本地构建推断，而是在发布产物级别被证实。**`npx @1.6.1` 实跑：目录 `my-app` + `--package-name other-name` → `name` 与 `<title>` 都是 `other-name`（目录名和模板原文 `Vite + Vue + TS` 都不是来源），README 新写的那句话在线上产物上成立。**CTV-32 第一次真实生效**：Release body 与发版前的 `--dry` 预演**逐字一致**，只有 📖 Documentation 一条；`ci:` 那条如设计所预期地不出现 |
| 2026-09-09 | CTV-38 / CTV-32 完成 | **CTV-38**：`--package-name` 的值同时写进 `package.json` 的 `name` 和 `index.html` 的 `<title>`，README 只说了前一半。选了补文档而非拆概念，另补一条 E2E 钉住这句话（目录 `my-app` + `--package-name other-name` → 断言 `<title>other-name</title>`，三个来源互相区分）。**测试是在红的状态下写的**——先注入变异让行为变错再写断言，2 个变异 2 个被捕获，还原后 `shasum` 与基线一致。**CTV-32**：新增 `changelogithub.config.ts`，收 refactor / docs / build，不收 chore / test / ci（仓库 11 条 chore 全是 `chore: release vX`）。默认三类是合并进来的、不是被替换，实测确认。**用真实 dry run 改前改后对照**：v1.6.0 那 4 个提交从只露 1 条 fix 变成 fix + 💅 Refactors + 📦 Build，chore 仍不出现；v1.1.0..v1.2.0 从 2 条变 6 条，CTV-15 的 265 行重构终于露面。**代价**：内部设计文档提交（`d0dd9a0`）也会进发布说明，以后这类改用 `chore(docs):`。验证：`build:prod` 退出 0（单测 239、E2E 90 → **91**），`npm pack` 仍 82 文件且配置未被打包，`dist/index.js` 用 stash 前后各构建一次比对**逐字节相同**。**已提交并推送**：`cabc2fb`（docs）+ `f289372`（ci），四个引用同步在 `f289372`，CI run 34299518308（main）与 34299518210（dev）均 success。**已随 v1.6.1 发布** |
| 2026-09-09 | **v1.6.0 发布** | ✅ npm 上线 1.6.0（`latest`），tag `v1.6.0` 与 GitHub Release 均已生成，Release workflow run 34291808422 success、CI run 34291805821 success。含 CTV-16 与 CTV-39 两条。**⚠️ 又是在 `dev` 上发的版，连续第二次**（v1.5.1 同样）——`main` 落后 4 个提交，已快进并推送，四个引用现在同步在 `7f8d509`，`git branch --contains v1.6.0` 已包含 main。同一个坑连踩两次，说明「发版前先切 main」靠人记不住，已开候选池 **CTV-40**（加脚本护栏）。**产物已实测**（registry 下载的真实 tarball + `npx @1.6.0`）：82 个文件 / 308 KB，**这是 `files` 改成通配后的第一次真实发布**——六个模板目录的 `package.json` 与 `_gitignore` 逐个点名核对全部就位，仓库 `src/`、`tests/`、`*.e2e.*`、`help.test`、vitesse、FUNDING、`.npmrc` 命中全为 0。**`npx` 实跑 16 项全过**：help 列出 11 个模板且派生列宽为 21（CTV-16）；安装失败时退出码 **7 原样透传**、末行 `└  依赖安装失败`、光标恢复、告知「项目已创建」并给出手动补装命令、项目未被回滚（CTV-39）；安装成功仍退 0 且末行 `└  程序结束`；内置模板从发布产物落地正常（`name` 与 `<title>` 改写、`_gitignore` → `.gitignore`）；`--version` 输出恰好 `1.6.0\n`。**Release notes 露出 1 条**（本批 1 个 `fix` + 1 个 `refactor` + 1 个 `build`，后两者不在 changelogithub 白名单里）。npm publish 未被 2FA 挡 |
| 2026-09-08 | CTV-39 完成 | 安装失败时不再跳过收尾兜底。`utils.ts` 私有的 `run()` 从 `process.exit(status)` 改成**返回状态**，`install()` 把它传回，`main()` 直接 `return status`——**至此 `src/` 里一个 `process.exit()` 都没有了**，CTV-34 那套「框线有主人」的保证再无缺口。**刻意没用抛异常**（那是我原先记在条目里的设想）：实测确认包管理器的退出码现在是**真的透传**的（退 7 原样出去 7），抛异常会让它一律变成 1，是行为倒退；`main()` 本就返回 `Promise<number>`，直接 return 更贴合现有形状。**顺带补上一个真实的体验缺口**（Hubery 拍板一起做）：装依赖失败时**项目其实已经建好了**，而此前用户完全看不出来——输出停在「使用 npm 安装依赖...」，后面只有包管理器自己的 stderr。现在会说清「项目已创建，但依赖没装上」并复用 `buildDoneMessage` 给出手动补装的命令。命令根本跑不起来那一支（包管理器没装）也不再打裸 Node 调用栈，立场同 CTV-31。E2E helper 的 `stubPackageManager` 扩成可指定退出码，**用 7 而不是 1**——1 和「取消」「参数有误」撞车，透传测试会分不清退出码是谁给的。验证：`build:prod` 退出 0，单测 239、E2E 82 → 90，真实跑过三种失败形态（退 1 / 退 7 / 命令不存在），框与光标全部收好、项目完好、退出码原样透传。**变异测试 12 个注入 12 个被捕获，零存活**——其中 Q06 第一版是**我的变异写错了**（让 `install()` 跑两次却仍返回状态，根本没测到「不传回状态」），改对后被捕获。**README 按发版清单同步了**：退出码表补一行「其它 = 透传包管理器退出码」，并说明装不上时项目不会被回滚 |
| 2026-09-08 | CTV-16 完成 | 模板清单不再手写。**加一个模板要改的地方从 4 处降到 2 处**（`FRAMEWORKS` + README），且漏改 README 会立刻红，漏改 help 与 `files` 在结构上不再可能。新增 `src/help.ts` 的 `renderTemplateList()` 由 `FRAMEWORKS` 派生「可用模板」那一块；它**不 import `constants`**（反向 import 会成环，数据靠参数传）。**排版刻意不复刻旧版**：旧版第一块列宽 14、custom 块 21、折行位置手挑，复刻那些等于把随意决定重新编码成数据；改成确定性算法（列宽 = 最长名 + 2 = 21，按 80 列折行，按框架分组上色），实际输出与旧版几乎一致。**「参数」那一块刻意没动**，属于另一件事且已有双向断言。**`files` 改通配时踩到一个只在真实 tarball 里暴露的坑**：`template-*` 让产物从 82 个文件掉到 **5 个**，模板目录**全部静默丢失**；实测五种写法后确认只有 `template-*` 加 `/**`（以及 `/*`）给出 82，裸目录名管用、通配到目录却不管用。为此新增 `tests/e2e/pack.e2e.test.ts`，**调真的 `npm pack --dry-run --json` 拿清单断言，而不是自己实现通配去猜 npm 的语义**——我第一版正是自己写了个匹配器，改成通配后它给出的判断是错的。通配的另一面风险（野目录被捎带）由 CTV-30 那条「不许有孤儿 `template-*` 目录」的反向断言挡着，两者配套。README 那份清单没法在构建期生成（静态 markdown），改为**加一条一致性断言**把静默漂移变成红测试；它不是恒等式——两边是不同的源（面向用户的文档 vs 注册表），已用两种漂移（删一行、写错名字）实测都能打红。验证：`build:prod` 退出 0，单测 229 → 239、E2E 78 → 82，`npm pack` 逐文件 diff 与改前**完全一致**（82 个，含 `_gitignore` 与 `.vscode`）。**变异测试 13 个注入 13 个被捕获，零存活**；`constants.test.ts` 里既有的三条模板断言**一字未改仍绿**，那是本次「行为等价」的钉子。**顺带修掉一个自造的小坑**：`pack.e2e.test.ts` 的注释表格里写了 `template-*` 加 `/**`，那个 `*` 加 `/` 把块注释提前闭合了，已转义 |
| 2026-09-08 | **v1.5.1 发布** | ✅ npm 上线 1.5.1（`latest`），tag `v1.5.1` 与 GitHub Release 均已生成，Release workflow run 34204524869 success、dev 上 CI run 34204520756 success。含 CTV-34 / 36 / 37 三条。**⚠️ 这次是在 `dev` 分支上发的版，踩中了发版清单第 1 条防的那个坑**——`bumpp` 把 `chore: release v1.5.1` 提交和 tag 都推到了当前分支，于是 `main` 落后一个提交、tag 也不在默认分支上。影响有限（差的只有 `package.json` 的版本号，代码完全一致），发现后已把 `main` 快进到 `dev` 并推送，四个引用现在同步在 `631d781`，`git branch --contains v1.5.1` 已包含 main。**教训重申**：日常在 dev、默认分支是 main，`pnpm release` 前必须先 `git checkout main`。**产物已实测**（对着 registry 下载的真实 tarball 与 `npx @1.5.1`，不是本地构建）：82 个文件 / 308 KB，仓库 `src/`、`tests/`、`*.e2e.*`、`terminal.test`、vitesse、FUNDING、`.npmrc` 命中全为 0，6 个 `_gitignore` 正常落地；`dist/index.js` 里「程序结束」「已中止」「转交给」「无效的 package.json name」各 1 处、`25h` 2 处。**`npx` 实跑逐条验过三个条目**：成功·不装依赖与成功·装依赖两支末行**同为** `└  程序结束`（形状一致，CTV-34）、转交上游先收框再交出终端且回到 finally、崩溃兜底末行 `└  已中止` 且错误信息仍在、非交互报错末行 `└  操作已取消` 且光标恢复（CTV-36）、非法 `--package-name` 的 stdout 为空且报错在 stderr（CTV-37）、`--version` 输出恰好是 `1.5.1\n` 不含任何转义序列（本轮自造回归的钉子）。**Release notes 露出 1 条**（本批 1 个 `fix` + 1 个 `test`，`test` 不在 changelogithub 白名单里），与预测一致。npm publish 未被 2FA 挡 |
| 2026-09-08 | CTV-34 / 36 / 37 完成 | 「退出前把终端还原成该有的样子」三条一起做。**三个条目各点了一处，实测有六处**——摸底跑遍 `main()` 的所有退出路径后发现：非法 `--package-name`（CTV-37）、成功·装依赖、成功·不装依赖（CTV-34 的两半）之外，**转交上游**和**崩溃兜底**两条框同样只开不关，而这两条三个条目一个都没点到。顺带纠正 CTV-36 记的一处：非交互路径我先前数出 `└2`，其中一个是半截 select 菜单自己渲染的底栏，框其实是关好的，只有光标没恢复。**所以修法不是补六处**，而是让「谁负责关框」有主人：新建 `src/terminal.ts` 持状态（`open` / `close` 幂等 / `markClosed` / `restoreCursor`），由 `runCli()` 的 `finally` 无条件兜底，`index.ts` 内部的两处 `process.exit()` 改成 `return`（它会跳过 `finally`，且仓库早有一条「`process.exit` 截断 stdout」的教训）。CTV-37 的修法是把 `--package-name` 的校验拆出 `resolvePackageName`（新增 `isArgPackageNameValid`）并提到开框之前——校验和 `targetDir` 无关，而推导要等它，挤在一起就必然先画框再打框外的报错。**做的过程中自己踩了一个回归并修掉**：`finally` 里无条件恢复光标，会让 `$(create-todo-vue --version)` 从 5 个字节变成 12 个（尾巴挂着 `ESC[?25h`），脚本直接被坑；改成「只在开过框之后才还原」，并补了一条整串相等的回归钉子。**E2E 补上一块长期留白**：helper 新增 `stubPackageManager`，在 PATH 最前面放一个立刻成功返回的假包管理器，于是 `install()`（`-i`）与 `customCommand`（转交上游）两条路径第一次有了 E2E 覆盖——边界是「测我们自己的收尾渲染」，不测包管理器能不能干活，CLAUDE.md 里那条「刻意留白」已按此改写。**变异测试 18 个注入、17 个被捕获**；唯一存活的 N07（去掉 `finally` 里的 `close()`）**经逐字节比对证明是等价变异**——9 条路径的 stdout/stderr/退出码完全一致，因为六条路径现在都自己收好了尾，那张网是给将来的新路径用的。**过程中变异先报了 6 个存活，逐个查下来分三类**：N09/N10/N11 是**我的断言太弱**（`outro('')` 渲染成 `└  `，只判 `/^└/` 分不出「收尾语丢了」；N11 那条更是**变异脚本本身写错**，只插了个 `void 0` 没真删代码），已改成整行相等；N13/N14 是**真盲区**——`process.exit` 跳过 `finally` 的差异恰好是 6 字节的 `ESC[?25h`，而 clack 的 spinner 自己也会写一个恢复光标，把 `show >= hide` 那种弱断言盖过去了，改成正面钉「开过框的路径必须以恢复光标收尾」后两个都被捕获；只有 N07 是真等价。验证：`build:prod` 退出 0，单测 216 → 229、E2E 78（新增 `tests/terminal.test.ts` 11 条 + `tests/e2e/frame.e2e.test.ts` 13 条），`npm pack` 仍 82 文件零混入，`--version` 回到 5 字节。**两条既有 E2E 因渲染变化被打红并已更新**（收尾信息改走 `log.info`，续行带 `│` 前缀，安装命令的整行匹配锚点要算上它——整行匹配的分辨力没有放宽）。**发现并记入候选池 CTV-39**（`install()` 失败时 `run()` 直接 `process.exit`，是这套兜底唯一的漏网之鱼，刻意没修）|
| 2026-09-08 | **v1.5.0 发布** | ✅ npm 上线 1.5.0（`latest`），tag `v1.5.0` 与 GitHub Release 均已生成，Release workflow run 34180271149 success、main 上 CI run 34180267299 success（发版前 f435f40 的两条 CI 34180077255 / 34180077234 也都 success）。含 CTV-31 与 CTV-35 两条。**产物已实测**（对着 registry 下载的真实 tarball 与 `npx @1.5.0`，不是本地构建）：82 个文件 / 308 KB，仓库自身 `src/`、`tests/`、`*.e2e.*`、vitesse、FUNDING、`.npmrc`、ROADMAP/CLAUDE 命中全为 0，6 个 `_gitignore` 正常落地；发布产物的 `dist/index.js` 里 `package-name` 4 处、「需要交互才能继续」「这一步在问」「非交互环境请改用」「会清空该目录」「会删除该文件」「无效的 package.json name」逐条齐全。**`npx` 实跑 8 项全过**：`--version` 报 1.5.0、`--help` 列出 `--package-name`、非空目录与包名两种卡点都打出人话并退 1（且 `keep.txt` 未被动）、`--package-name` 让 `.foo` 全程无交互建成、显式优先生效、非法值报错且不创建任何东西、`--packageName` 被当未知参数挡下。**Release notes 露出 1 条**（本批只有 1 个 `feat`，`docs` 与 `chore: release` 照旧不在 changelogithub 白名单里），与发版前的预测一致。这次没被 2FA 挡，tag 与 npm 一次到位，没有出现 v1.3.0 那种不一致窗口。发版后 `dev` 已快进到 `main`，四个引用全部同步在 `1825173`。**收尾验证时发现两笔小账，均记入候选池**：CTV-37（非法 `--package-name` 的报错打断 clack 框线，且我在 CTV-31 里为它写的「与未知参数形态一致」那句注释经实测是错的）与 CTV-38（`--package-name` 同时改写 `<title>`，README 少说了一半）。两条都不影响退出码与产物正确性 |
| 2026-09-08 | CTV-31 / CTV-35 完成 | 非交互环境下不再「画个菜单然后静默死掉」。**条目原文点名的做法（检测 `process.stdin.isTTY`）被实测证伪**：管道也不是 TTY（`stdio:'pipe'`、shell 的 `echo x \|`、`stdio:'inherit'` 下 `isTTY` 一律 `undefined`），按它判会打红 E2E 里 4 条靠管道喂按键的用例，还会误伤真实的 `echo \| cli`。改用的判据是**「prompt 还挂着的时候 stdin 抵达 EOF」**——新增 `src/interactive.ts` 的 `ask()` 用 `Promise.race` 让 prompt 和 stdin 的 EOF 赛跑，EOF 赢就抛 `NonInteractiveError`，由 `runCli()` 分流出来打人话、**不打调用栈**（参数没给全不是 bug）。7 个提问点各传「这一步在问什么」+「非交互下改用哪个参数」，所以报错能直接告诉用户加 `--overwrite` / `-t` / `-i` / 位置参数。**实测确认挂 'end'/'close' 监听不会把字节从 clack 手里抢走**（只有 'data' 监听和 `resume()` 会让流进入流动模式），那 4 条管道用例一字未改仍全绿，它们同时就是这条不变量的回归钉子。**B1 的 `--package-name` 补上了最后一个洞**：此前包名是唯一给不出参数的提问点，目录名推不出合法包名（`My App` / `.foo`）时非交互调用必然卡死且无药可救。语义定为「显式优先于推导 + 非法值报错而不静默修正 + 空串按非法处理」。**实现中发现并修掉一个次序缺陷**：包名校验原本排在第 2 步之后，于是 `--overwrite` 会先把目录清空、再告诉用户参数写错了——用户为一个打字错误付出整个目录；已把解析提到破坏性操作之前（提问仍留在第 3 步，交互次序不变），并加了专门的 E2E 次序守卫。**变异测试 19 个注入 19 个被捕获，零存活**；其中 M03（只监听 'end' 不监听 'close'）**一度存活，核实为真盲区而非等价变异**——实测 `destroy()` 只发 `['close']`、`destroy(err)` 发 `['error','close']`，都没有 'end'，对应 stdin 被关掉 / 读 stdin 报 EIO 的真实场景，补一条用例后被捕获。**还纠正了三条写歪的断言**：「点名卡在哪一步」原本只断言 `选择模板`，那四个字在半截菜单里本来就有（恒等式）；「带了参数没带值」原本靠「`--package-name` 是未知参数」绿着；「不打调用栈」原本在「压根没抛异常」的旧行为下也绿。**两处既有注释仍全绿但理由已失效**（`cli-basics` 与 `target-path` 里写着「不要断言任何提示文案，这条路径一个字都不打」），已按 CTV-30 的先例改写而不是放着。验证：`build:prod` 退出 0，单测 198 → 216、E2E 50 → 65，`npm pack` 仍 82 文件且零测试混入，真实 CLI 跑过两种卡点。README 同步：`--package-name` 进参数说明与快速创建示例、退出码那节补一条、顺带修掉「两点值得单独说明」与三条列表对不上。**顺带做了 CTV-35**（README 那句「交互式选择 Vue 时」，独立提交）。**发现并记入候选池 CTV-36**（非交互报错后光标没恢复，改动前就存在，非回归）|
| 2026-09-08 | CTV-31 的卡点被证伪 | 条目原文说纯脚本模式「需要把所有 prompt 都做成可跳过」，据此判成大改动而一直搁着。逐个查过 `src/index.ts` 的 7 个提问点后发现**6 个已经可跳过**（121 项目名 / 156 同名文件 / 186 非空目录 / 245 选框架 / 262 选变体 / 309 装依赖，各有对应的 flag 或位置参数），E2E 那 50 条用例走的就是这条全程无提问的路径。**唯一的洞是 223 行的包名**——条件是 `derivePackageName()` 的 `needsPrompt`（`!isValidPackageName(basename)`），只在目录名推不出合法包名时触发。于是「只报错」与「支持纯脚本模式」的成本几乎拉平，决策重写成 A / B 两个方案。**教训**：待办条目里的成本估计也会过期，尤其是跨了几次重构之后——CTV-15 把 `index.ts` 拆成三层时顺带把这些提问点的守卫条件理清楚了，但没人回头改 CTV-31 的描述 |
| 2026-09-08 | **v1.4.0 发布** | ✅ npm 上线 1.4.0（`latest`），tag `v1.4.0` 与 GitHub Release 均已生成，Release workflow run 34172214067 success、main 上 CI run 34172211802 success。含 CTV-30 一条（**刻意单独发**）。**产物已实测**（对着 registry 下载的真实 tarball 与 `npx @1.4.0`，不是本地构建）：82 个文件 / 304 KB，vitesse 目录 / `tests/` / FUNDING 均为 0，6 个 `_gitignore` 正常落地成 `.gitignore`；发布产物的 `dist/index.js` 里两条 degit 指令与两个变体名齐全、**已迁移的旧地址 `antfu/vitesse` 零残留**；`npx` 实跑 `--version` 报 1.4.0、`--help` 列出全部 11 个模板（含 5 个 `custom-*`）、内置 `vue-ts` 的 `name` 与 `<title>` 改写正常、**`-t custom-vitesse` 从已发布的包真的转交成功**（克隆 81 文件、带 `pnpm-workspace.yaml`、退 0）、无效模板名退 1。**这次没被 2FA 挡**，tag 与 npm 一次到位，没有出现 v1.3.0 那种不一致窗口。**Release notes 只露出 1 条**（本版就只有 1 个 `feat` 提交，`chore: release` 照旧不在 changelogithub 白名单里），与发版前的预测一致。发版前按清单查过 README：新增两个模板行 + 一段「转交上游不改写 name/title、需联网」的说明。发版后 `dev` 已快进到 `main`，四个引用（main / dev / origin/main / origin/dev）全部同步在 `84b6916` |
| 2026-09-07 | CTV-30 完成 | vitesse 从「仓库内置两份坏副本」改成「转交上游」。**上了两个变体不是一个**：下架的是两个目录，上游 `antfu-collective/vitesse` 与 `-lite` 都还活着（`antfu/vitesse-lite` 经 `gh api` 实测 302 到新址），只恢复一个会让「lite 去哪了」无解。**条目里「需同步 HELP_MESSAGE」这句是基于错误前提写的**——help 的「可用模板」原本只列 6 个内置模板，现存三个 `custom-*` 一个都没列，而它们都是 `-t` 的合法值；按 Hubery 决定一并补全全部 5 个。**三条测试是这次自己加出来的**，因为原方案有三块零覆盖：① 孤儿 `template-*` 目录的反向断言（正向那条只管「注册表里的模板有目录」，删目录这个方向一条测试都碰不到）；② help 覆盖断言从「内置模板」扩到全部 `TEMPLATES`，**并从子串匹配改成按词匹配**——`custom-vitesse` 是 `custom-vitesse-lite` 的子串，照抄子串写法会让「漏掉短的那个」全绿蒙混过去（M3 实测证实）；③ 切词数量断言。**一处自我纠正值得记**：给第一层选择器写的 E2E 断言起初把期望值写成 `FRAMEWORKS.map(f => f.display)`，变异 M7（`Vue` → `Vue.js`）**存活**——派生期望值会跟着输出一起变，对注册表内容天生没有分辨力；改成硬编码 `['Vanilla','Vue','Lit']`（与 `constants.test.ts` 硬编码 TEMPLATES 一致）后 M7 被捕获。**另有两条既有 E2E 被本次改动打红/打成过期的绿**：`帮助里列出的模板都是真实存在的内置模板` 真红了（help 现在也列 `custom-*`，它们本就不该有目录），已按类型分开断言而不是放宽；`not.toContain('vitesse')` 那条仍全绿但理由已失效，实测确认它通过的真正原因一直是「变体那层压根没渲染」，已换成钉真不变量。验证：`build:prod` 退出 0，单测 182 → 198、E2E 50 条不变，`npm pack` 仍 82 文件 / 511 KB / vitesse 命中 0，**9/9 变异被捕获**。**联网实跑两个变体**（E2E 按设计不覆盖 customCommand，这是唯一能拿到的证据）：npm 环境下 `custom-vitesse` 克隆 81 文件、pnpm 环境下 `custom-vitesse-lite` 走 `pnpm dlx` 克隆 40 文件，退 0。**顺带证伪了 CTV-01 的病因在上游存在**：上游两个仓库都自带 `pnpm-workspace.yaml`，50 / 26 处 `catalog:` 引用逐条比对全部可解析——当初是拷贝副本时漏了定义源。**degit 还绕开了 CTV-12 那条约束**：走 git 不走 npm 打包，`.npmrc` 与 `.gitignore` 原样落地。新开候选池 CTV-35 |
| 2026-09-07 | **v1.3.0 发布** | ✅ npm 上线 1.3.0（`latest`），tag `v1.3.0` 与 GitHub Release 均已生成，Release workflow run 34095685873 success（43s）、main 上 CI run 34095682645 success。含 CTV-19 / 20 / 21 / 18。**产物已实测**（对着 registry 下载的真实 tarball 与 `npx @1.3.0`，不是本地构建）：82 个文件、vitesse / `tests/` / FUNDING 均为 0、6 个 `_gitignore` 正常落地成 `.gitignore`；绝对路径产物落位正确且 cwd 无镜像树、文件目标 `--overwrite` 退 0 且零 ENOTDIR、装完依赖打出启动命令、`name` 与 `<title>` 改写正常、五种退出码（help/version/正常/拼错参数/无效模板）全部符合预期。**npm publish 被 2FA 挡了一次**，由 Hubery 用 `--otp` 补发——此时 tag 与 Release 已经生成而 npm 还是 1.2.0，正是 2026-09-05 记过的那种不一致状态，只是这次几分钟内就收口了。**Release notes 这次露出 4 条**（1 feat + 3 fix，含 v1.2.0 之后补的 mri 修复），比前两版的各 2 条好——原因是这批 fix 多，`refactor` 与 `docs` 照旧不在 changelogithub 白名单里，与此前实测一致。发版前按清单同步了 README：**位置参数此前从未被文档化**，而这版恰好改了它的行为，故补上「相对/绝对路径都可以」，并把 `--overwrite` 的说明从「目标目录不为空」扩到「目录清空（`.git` 保留）/ 同名文件删除」。发版后 `dev` 已快进到 `main` |
| 2026-09-07 | CTV-18 / 19 / 20 / 21 完成 | 四条一批，共用 `src/index.ts` 收尾那段代码。**验证**：`build:prod` 退出 0（typecheck / lint:fix / 182 单测 / build / 50 E2E 全过），单测 166 → 182、E2E 39 → 47 → 50，`npm pack` 仍是 82 个文件且 `tests/` 零混入，四条改动各自真跑 CLI 实测过。**变异测试 17 个注入、16 个被捕获**；唯一存活的 M02（`pathKind(root)` 改回 `pathKind(targetDir)`）是**事先预判并事后证明的等价变异**——`cwd` 在 `index.ts:78` 一次性取自 `process.cwd()`，`src/`、`bin/` 零 `chdir`，五种路径形态实测恒同址；保留 `root` 写法仍更好（语义直白、将来引入 chdir 也不会错）。两个变异同时被 `tsc` 拦下（M07 / M13），但都另有 unit 或 e2e 捕获，没有任何一条只靠编译期。**顺带补上一处 CTV-29 当时记为「进不了 E2E」的洞**：E2E helper 新增 `respondAfterStdout`，可以往管道里喂按键在选择器里**真的选中某一项**（`\r` 选第一项、`\u001B[B\r` 选第二项），于是「用户主动选取消」那半边分支第一次有了真覆盖——此前那类断言全是结构性假绿（clack 在非交互下 promise 永不 settle，`await` 之后一行都不执行，删掉整段取消逻辑测试照样绿，M08 实测证实了这一点）。**发现并新开 CTV-34**（`intro()` 框线两头都没收好），按 Hubery 决定单开单做 |
| 2026-09-07 | 排期表重构 | 版本桶漂过两次（「v1.2.0 测试与 CI 地基」实际随 v1.1.0 发布；「v1.4.0」桶里的 CTV-15 / 17 与候选池的 CTV-29 实际发成 v1.2.0），根因是 semver 由改动性质决定、不由计划决定。改为：**已发布的按真实版本归档，未发布的按主题分组 + 优先级，发版时才定版本号**。同时依据实测**上调 CTV-19 的优先级**——它不是「不支持绝对路径」，而是会在 cwd 下造出一整棵镜像目录树、用户要的位置根本不存在，却报告创建成功。新增候选池 CTV-32（changelogithub 类型白名单）与 CTV-33（把模块级常量交给第三方库的同类风险） |
| 2026-09-07 | 发版后修 `mri` 污染 | `f73a15e`。CI 在 main 上转红而同一提交的 Release workflow 却通过——根因是 **mri 会就地改写传入的配置对象**：alias 的值变成数组、`alias.help` 变成 `[]`、`boolean` 每次调用追加一轮且无上限（实测 4 次调用后从 4 项涨到 13 项，测试进程里涨到 193 项）。CTV-17 把模块级的 `ARGV_OPTIONS` 直接传了进去，`collectKnownFlags()` 随后读到被污染的数据。**本地与 dev 的 CI 都绿**是因为 `constants.test.ts` 的清单在 describe 体里算、跑在收集阶段，而 CI runner 的并行度让 `entry.test.ts` 先整个跑完；用 `--no-file-parallelism` 拿到了确定性复现。修法是传 `structuredClone` 副本——**`Object.freeze` 无效**：mri 是 CJS 非严格模式，赋值只静默失败，且浅冻结管不到嵌套的 alias 与 boolean（已实测，本来打算加，验证后否掉）。**线上 v1.2.0 未受影响**（实测 8 个参数全部正常、退出码 1/1/0 正确），因为真实进程只解析一次、那一轮污染后字符串成员仍然齐全——是运气不是设计。补 2 条回归测试，断言写成绝对值而非「调用前后相等」 |
| 2026-09-07 | **v1.2.0 发布** | ✅ npm 上线 1.2.0（`latest`），tag 与 GitHub Release 均已生成，Release workflow run 34085371832 成功。含 CTV-15（`index.ts` 拆分）、CTV-17（未知参数校验）、CTV-29（取消退出码）。**产物已实测**：从 npm 装 1.2.0 后 8 个参数全部正常、`--overwirte` 正确报未知、取消退 1、正常创建退 0、`name` 与 `<title>` 改写正常。⚠️ **Release notes 只露出 2 条**——`refactor` 不在 changelogithub 默认白名单里，CTV-15 那次 265 行的重构完全隐形（此前我误判它会被收录，已核实更正，见候选池 CTV-32）。发版前按清单补了 README 的「退出码」一节；发版后 `dev` 已快进到 `main` |
| 2026-09-07 | CTV-17 完成 | 未知参数不再被静默吞掉。设计核心是**防漂移**：mri 配置提成 `ARGV_OPTIONS`，校验名单由 `collectKnownFlags()` 从它派生而非手抄第二份。别名两侧都要算已知（变异 #6 证实：丢掉短名一侧会让 7 条测试转红）。**顺带补了本条目自己制造的新风险的守卫**：加严格校验后，`HELP_MESSAGE` 写了而 `ARGV_OPTIONS` 没声明的参数会被 CLI 拒绝，于是在 `tests/constants.test.ts` 双向钉住两者。**次序守卫值得单说**：`--help` 必须优先于校验，为此写的两条用例在实现前是**假绿**的，靠变异 #2 才证明有牙齿。11/11 变异被捕获 |
| 2026-09-07 | CTV-17 完成 | 未知参数不再被静默吞掉。设计的核心是**防漂移**：mri 配置提成 `ARGV_OPTIONS`，校验名单由 `collectKnownFlags()` 从它派生而不是手抄第二份。别名两侧都要算已知——mri 解析 `-t vue` 会同时产出 `t` 和 `template`，只收一边会把合法输入误判成未知（变异 #6 证实：丢掉短名一侧会让 7 条测试转红）。**顺带补了一条本条目自己制造的新风险的守卫**：加了严格校验后，`HELP_MESSAGE` 里写了而 `ARGV_OPTIONS` 没声明的参数会被 CLI 拒绝——等于拒绝自己文档宣传的东西，于是在 `tests/constants.test.ts` 双向钉住两者。**次序守卫值得单独一提**：`--help` 必须优先于校验，为此写的两条用例在实现前是**假绿**的（没有校验时它们必然通过），靠变异 #2（把校验挪到 help 之前）证明它们真有牙齿。11/11 变异被捕获 |
| 2026-09-07 | CTV-29 完成 | 取消操作不再退 0。**设计做到一半被实测推翻**：原方案只打算把 `return cancel()` 改成返回 1，但探针发现 `@clack/core@1.4.3` 的 prompt 在 stdin 不可读时 **promise 永不 settle**——`await` 之后一行都不执行，`isCancel` 分支和 `cancel()` 从来没被调用过；进程是靠事件循环排空自然退出的（Node 在顶层 await 下会明说 `Detected unsettled top-level await` 并退 13，我们的 await 在 `main()` 内所以毫无提示）。也就是说原方案只能修好交互式 Ctrl+C，修不了条目点名的脚本化场景。补法是在 `runCli()` 预置悲观退出码。**另一处自我纠正**：起初写的 4 条 E2E 里有一条断言取消时打印「操作已取消」，那句话在非交互路径上永远不会出现，是个假需求，已删。**还有一处不接受的缺口被补上**：变异测试显示「`cancelled()` 改成返回 0」能全绿存活，说明交互式那半边零覆盖——探明 clack 在管道下仍靠 readline 的 keypress 识别 `\u0003`，于是给 E2E helper 加了 `cancelAfterStdout`（等某段输出出现再喂 Ctrl+C，不用定时器以免 flaky；写完必须 end() 否则管道开着进程不退），补上第 4 条用例后该变异被捕获。最终 5/5 变异被捕获。顺带发现并记入候选池 CTV-31（非交互时画个菜单然后静默死掉，该给人话）| 
| 2026-09-07 | CTV-15 完成 | `src/index.ts` 从 265 行拆成三层：`plan.ts`（纯决策，8 个函数）+ `scaffold.ts`（副作用）+ `index.ts`（编排 + 交互，导出 `main()` / `runCli()`），`bin/index.js` 改为显式调用。范围**刻意限定为纯重构**，CTV-17/18/19/20/21/29 一条没碰——行为零变化时「E2E 31 条一字不改全绿」才直接等价于「没弄坏东西」。证据：既有 4 个测试文件 0 处改动、`build:prod` 退出 0、单测 77 → 142、E2E 31/31、重构前后 48 个产物文件 `diff -r` 零差异。**变异测试 22/22 被捕获，无存活、无等价变异**——但第一版脚本误报过一条「存活」，根因是变异串在 JSDoc 里也出现了一次，`replace(...,1)` 打在注释上、代码没动；已加「必须恰好命中一次」的守卫。另一处修正：新增的 `tests/entry.test.ts` **不是**「禁止顶层自执行」的守卫（实测把 `main()` 加回去它仍全绿，因为首次 import 时自执行已发生而那条用例只断言导出类型），真正守着这条不变量的是 E2E（实测会红 2 条），已写进该文件注释 |
| 2026-02-09 | v1.0.0-beta.1 / beta.2 | 早期迭代 |
| 2026-03-05 | v1.0.0 / v1.0.1 | ✅ 发布到 npm |
| 2026-09-04 | 首次全量审查 | 摸清架构（bin → dist → 顶层自执行 `init()`、FRAMEWORKS 两层树、customCommand 与内置模板两条互斥路径）。查出 1 个用户可见破损（vitesse 模板 catalog 无定义源）、1 个发布期才暴露的坑（npm 剔除 `.npmrc`）、1 个合规问题（模板带上游作者的 FUNDING/LICENSE），以及零测试、零 CI 两项地基缺失 |
| 2026-09-06 | **v1.1.0 发布** | ✅ npm 上线 1.1.0（82 文件 / 507 KB），tag `v1.1.0` 已推送，`release.yml` run 34067855422 全 12 步 success（35s）——tag↔version 校验与 changelogithub 两段**首次真实运行**，GitHub Release 已自动生成。产物验证是对着真正发出去的 tarball 做的：vitesse 相关 0 个、FUNDING.yml 0 个、6 个模板目录、dist 就位；并用 `npx @huberyyang/create-todo-vue@1.1.0` 真实生成了一个 vue-ts 项目。⚠️ Release notes 只列出 2 条——changelogithub 默认过滤掉 `docs`/`ci`/`build`/`test`/`chore`，7 个提交里 5 个没露面 |
| 2026-09-06 | v1.3.0 方向变更 | 原计划修好 vendor 的 vitesse 副本再上架（CTV-11~14），查证后改为删副本、转交上游（新条目 CTV-30）。决定性事实：副本 568 KB / 115 文件 / 73 依赖，且来自已迁移的旧仓库地址 `antfu/vitesse`（上游现为 `antfu-collective/vitesse`），维护成本会随上游演进反复产生；而 `customCommand` 机制现成可用、零新代码。CTV-11~14 标 ❌ 保留在表里，编号不回收 |
| 2026-09-06 | 发版收尾 | `dev` 快进到 `main`（bumpp 的发版提交只打在 main 上，不同步的话下一轮开工版本号是错的）。README 同步更新：补 `-v, --version` 与 `--no-immediate` 说明、快速创建给出「装/不装依赖」两个示例、加 CI 徽章、版本徽章改读 npm；**顺带修掉一个一直没人发现的错误——license 徽章指向的是 `npm/l/express`**，显示的是别人包的许可证。README 里每条 flag 都对着已发布的 1.1.0 实测过。**并把「每次发版必查 README」写进了 CLAUDE.md 的发版清单和本文件的维护规则第 4 条** |
| 2026-09-05 | 分支同步 | `main` 从 v1.0.1 的 `42adac8` 快进到 `2442ed5`，与 `dev` 一致（无分支保护，clean fast-forward）。main 上的 CI 跑绿（run 33933049790，36s）。**发版被 npm 认证挡住**：本地存的 token 返回 401，`npm whoami` / `pnpm whoami` 都失败，故未执行 bumpp——否则会推出 tag、生成 GitHub Release，而 npm 上仍是 1.0.1，形成更糟的不一致状态 |
| 2026-09-05 | CTV-08 完成 | E2E suite 落地：`vitest.e2e.config.ts` + `tests/e2e/`（global-setup 先构建 dist、helpers 里的 fixture 与环境白名单），31 条用例 1.6s。不引入 execa，用 node 内置 `spawn` + `stripVTControlCharacters`。E2E 不进 pre-commit，接进 `build:prod` 与 ci/release 两个 workflow 的门禁。9 个变异里 8 个被捕获，第 9 个核实为等价变异。**顺带修正一处上一轮的错误结论**：`managePackageManagerVersions` 并没有被 `pnpm-workspace.yaml` 接受（pnpm 每条命令都警告 not recognized），我当时看 `pnpm config get` 返回 true 就判定生效，那其实是 pnpm 11 的默认值。已删掉该行——实测把 packageManager 改成不存在的版本，pnpm 仍会去 registry 拉取，证明该行为默认开启，那一行本就多余。发现 CTV-29 |
| 2026-09-05 | 升级后 CI 跑绿 | run 33930918212，gate 30s success（升级前 20s）。确认：CI 用 pnpm 11.25.0、typescript 6.0.3 被真实安装（typecheck 不再依赖 runner 的全局 tsc）、77 条测试全过。**修正一处此前的估计**：`trustPolicy` 的供应链校验在 CI 里只用 6.6s（559 条），不是本地冷缓存下测到的 18s，不值得为它关规则 |
| 2026-09-04 | 全量依赖升级 | pnpm 10.30.3 → 11.25.0，8 个 devDependency 跨大版本（commitlint 20→21、bumpp 10→12、lint-staged 16→17、tsdown 0.20→0.22、vitest 4→5、todo-scripts 1.0→1.3、@types/node 25→26、eslint 10.0→10.9），新增 typescript 6.0.3（解决 CTV-27）。配置侧连带改动：① pnpm 11 不再读 package.json 的 `pnpm` 字段 → 新建 `pnpm-workspace.yaml`；② 构建脚本未放行从警告变报错 → `allowBuilds: {esbuild: true}`（注意是 `allowBuilds` 不是旧的 `onlyBuiltDependencies`）；③ `.npmrc` 删除，唯一那条设置迁进 workspace yaml，顺带消掉 npm 的 Unknown project config 警告；④ eslint-plugin-pnpm 强制的三条设置；⑤ tsdown 0.22 的 `inlineOnly` → `deps.onlyBundle`；⑥ 移除 `npm-run-all2`（新版要 Node ^24.15.0，本机 24.13.0）改用 `&&` 串联，`preview` 也一并从全局 `nr` 换成 `pnpm`——现在所有脚本零全局依赖。`engines` 刻意保持不变：产物只引用 node: 内置模块，Node 20 用户不受影响。验证：install/frozen-lockfile/typecheck/lint/test/build 全 0，77 条测试全绿，CLI 真实生成项目成功，lint-staged 17 读得懂现有配置 |
| 2026-09-04 | CTV-07 完成 | `src/utils.ts` 单测从 0 补到 71 条，覆盖 12 个导出函数；`getFullCustomCommand` 按 7 种包管理器状态 × 3 种指令形态铺成 21 行矩阵，另有一条守卫用例确保 FRAMEWORKS 里不会冒出矩阵没覆盖的第四种指令形态。**关键动作是变异测试**：给 `src/utils.ts` 注入 13 个变异（pnpm 走 `--`、yarn1 不剥 `@latest`、yarn1 用 dlx、isEmpty 不放过 `.git`、copy 不识别目录……），13/13 全部被断言捕获，证明这套表征测试不是摆设。顺带发现 CTV-27（typescript 未声明） |
| 2026-09-04 | CI 首次真实跑绿 | 推送 dev（3 个提交）触发 `ci.yml` run 33871696605，gate job 20s success。顺带验证掉三件此前只能靠推理的事：`action-setup` 排在 `setup-node` 前面确实让 `cache: pnpm` 生效、`--frozen-lockfile` 通过说明改 package.json 没让 lockfile 失同步、`lts/*` 满足 engines 约束 |
| 2026-09-04 | CTV-09 / 10 / 26 完成 | 移植 todo-scripts 的发版流程。新建 `release.yml`（tag 触发 + dispatch 补发 + tag↔version 校验 + 四道门禁 + changelogithub@15）与 `ci.yml`（push/PR 跑同一套门禁）。前置改动：`test` 改 `vitest run` 并加 `test:watch`（CTV-07 的一部分提前做）、`build:prod` 补上 test、删掉与 lifecycle 同名的 `publish` 脚本（CTV-26）、lint-staged 配置外置到 `lint-staged.config.mjs` 并对齐 todo-scripts 的三件套。验证：`pnpm install --frozen-lockfile` 退出 0、`pnpm test` 3s 自行退出（改前是 watch 会挂死）、`build:prod` 四步按序全跑通、两份 YAML `yaml.safe_load` 解析通过、tag 校验逻辑三条分支（匹配/不匹配/缺 v 前缀）实测符合预期、三个 action 版本经 `gh api` 核实存在。**未推送，workflow 未经真实运行验证** |
| 2026-09-04 | CTV-02 / 03 / 04 / 06 完成 | v1.1.0 全部条目做完。CTV-02 顶层 `.catch()` + `spin.error()` + 补 `intro()`；CTV-03 `-v/--version`，靠新的 `findPackageJson()` 向上查找而非写死层级；CTV-04 `lint-staged` 去掉那个会拖全仓的 `.`、补 `.vscode/extensions.json`；CTV-06 删掉空转的 `auto-update-readme.yml`。验证：vitest 12/12 绿、typecheck 绿、lint 绿、build 绿，`--version` 输出 1.0.1，真实生成 vue-ts 项目成功，构造 ENOTDIR 实测报错首屏变成 clack 格式而非 node 内部栈 |
| 2026-09-04 | CTV-01 完成 | 下架 vitesse 两个模板（`FRAMEWORKS` / `HELP_MESSAGE` / README / `files` 四处），目录保留待 v1.3.0 修复。新增 `tests/constants.test.ts` 6 条断言钉住模板注册表——含「内置模板 package.json 不含 `catalog:`」这条，直接把 CTV-01 的病因变成回归测试。验证：vitest 7/7 绿、typecheck 绿、lint 绿、`node bin/index.js my-app -t vue-ts` 真实生成成功、`npm pack --dry-run` 产物 82 个文件且 vitesse/FUNDING 命中数为 0 |
| 2026-09-04 | 建立本工作流 | 落地 CLAUDE.md + ROADMAP.md + `docs/superpowers/specs/`，对齐 todo-scripts。整理出 26 条待办，切分为 v1.1.0 ~ v1.4.0 四个版本 + 4 条候选池 |
