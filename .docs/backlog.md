# 候选池与待决策

会话中冒出的新想法先落这里。**进不进待办、排在哪个主题、优先级多高，都是 Hubery 的决定** ——
Claude 不擅自把条目从这里挪进 [roadmap.md](./roadmap.md) 的待办分组。

> 本文件随仓库走（2026-09-21 起 `.docs/` 已纳入版本管理，`package.json` 的 `files` 不含它，不会发进 npm 包）。

---


没排进待办的想法都堆在这里。**优先级与版本归属由 Hubery 决定，Claude 只负责往这里追加。**

| 状态 | ID | 条目 | 卡在哪 |
|:--:|:--|:--|:--|
| ❌ | CTV-23 | ~~npm Trusted Publishing~~ | **2026-09-09 放弃**：Hubery 决定发版流程维持现状——`npm publish` 继续由他在本地跑。与 todo-scripts 的 HB-30 （2026-09-07 同样放弃）保持一致，两个仓库不再分叉。**已知且接受的代价**：npm 上的包继续没有 provenance 证明。**决策依据（本次查证得到的硬事实）**：provenance **只能**由 CI 的 OIDC 生成，本地 `npm publish` 无论加什么参数都拿不到——所以「保留本地 publish」与「产物带 provenance」是二选一，不存在中间态。一并放弃的还有两项顺带收益：发版不用再输 OTP、以及消除 CLAUDE.md 记的「bumpp 推完 tag 但 publish 卡在 OTP」那个不一致窗口（v1.3.0 真踩过）。**条目原文「两个仓库可以一起迁」这半句在本次核实中已失效**（todo-scripts 早两天就放弃了），已改写。**前置条件留档，哪天重开直接可用，不必重新调研**：① `repository.url` 与 `GITHUB_REPOSITORY` 字符串一致（都是 `Hub-yang/create-todo-vue`，`gh api` 确认仓库没转移过）——todo-scripts 栽过的那条**这里天然通过**；② 仓库 public、包 public，满足 provenance 前提；线上 `dist.attestations` 实测为空，确认现状确实没有；③ npm ≥ 11.5.1、Node ≥ 22.14（本机 npm 11.6.2 / Node 24.13.0 满足；**CI 的 `lts/*` 带的 npm 版本要打印实测，不许推理**）；④ `permissions` 加 `id-token: write`，`contents: write` 要保留给 changelogithub；⑤ publish **不需要** `--provenance` 标志、也**不需要** `NODE_AUTH_TOKEN`，OIDC 全自动（2026-09-09 查官方文档，比 memory 留档新）；⑥ npmjs.com 上登记 trusted publisher 填：org/user + repo 名 + **workflow 文件名**（只填 `release.yml`，不是完整路径，含扩展名、大小写敏感），environment 可选。**保存时不校验，填错只在发布那一刻报 403**；⑦ 每个包最多 10 个 trusted publisher，已建立的连接**不能改只能删了重建**；⑧ CI 里必须用 `npm publish` **不能用 `pnpm publish`**（pnpm 自 11.0.8 起 OIDC publish 有回归，pnpm#11513 / PR #11526，本仓库 pin 的 11.25.0 是否含该修复未确认）；⑨ workflow 需要显式 `pnpm build` 而不是靠 e2e 的 globalSetup 顺带产出——**本仓库已经有这一步**，todo-scripts 当时没有；⑩ publish 要做幂等保护：`workflow_dispatch` 补发路径会重跑 publish，版本已存在 → 403 → 把补发能力废掉，publish 前要先查 registry |
| ✅ | CTV-22 | **生成项目后自动 `git init`** | **2026-09-09 完成。条目原文的依据「create-vue 做了」已失效**——当天查上游 `index.ts` 确认它**也不执行**，只在收尾提示里给一句建议命令，且仅当目标不在 git 仓库内时才显示。Hubery 明确「确定要做」，据此照做，并借鉴了它那个「先检测 `.git`」的思路。**做法**：`utils.ts` 新增 `findGitDir()`（照抄既有 `findPackageJson()` 的向上查找形状，用 `existsSync` 以兼容 worktree/submodule 里 `.git` 是文件的情形）与 `gitInit()`（复用私有 `run()`，`stdio: ignore` 免得 git 的输出撞乱 clack 框线）。**只 `git init`，不 add 不 commit**（Hubery 选定）——省掉对 user.name/user.email 的依赖。接线排在 `scaffoldTemplate()` 之后、第 5 步之前，**位置是设计的一部分**：装依赖失败那一支直接 `return status`，放后面会让「装依赖失败但项目已建好」的路径跳过 init。已在 git 仓库内则跳过并提示（Hubery 选定）。**失败只 warn、不改退出码**，与 install 失败的透传刻意不同——git init 是附加动作，让它把成功变成失败是撒谎。`custom-*` 不做。**测试基础设施改了一处**：`fixture.tree()` 不再列 `.git`（理由见 CLAUDE.md），**加这条排除时既有 91 条一条没变**，是干净信号。验证：新增 `git-init.e2e.test.ts` 4 条（含一条反向钉子「正常情况下不该打印跳过提示」），**先写先红**（3 红 1 绿，那 1 绿正是反向钉子），实现后转绿（成本：中） |
| ❌ | CTV-24 | ~~renovate 要不要管 `template-*/package.json`~~ | **2026-09-09 处置：不用 renovate，删掉配置**（`.github/renovate.json5`）。**条目原来的问题问错了**——它假设「renovate 现在会扫到模板的 package.json 并提升级 PR」，于是把选择摆成「配 `packageRules` 分组 vs 配 `ignorePaths`」。核实发现**这件事从未发生**，两重证据：**①从未运行**——配置加于 2026-03-05（`3b75bd4`），半年里 0 个 PR、0 个 issue（`config:recommended` 默认会建 Dependency Dashboard）、0 个 `renovate/` 远端分支、0 条 renovate 提交，而它自己开着的 `lockFileMaintenance` 本该每周一跑；`schedule` 窗口也确实到过（2026-09-06 是周日）。`.github/workflows/` 里也没有 self-hosted 的 renovate action，即 App 从未安装。**②配置本身是坏的**——`npx --package renovate -- renovate-config-validator` 实测报 `Invalid schedule: Failed to parse "after 10pm every Sunday and before 5am every Monday"`，装了 App 也只会得到配置错误。所以删它不是「关掉一个功能」，是清掉一份从未运行、运行也会报错的死配置。**依赖升级维持现状**：`pnpm taze` 手动跑。**顺带对照**：todo-scripts 那边压根没有 renovate 配置文件，所以这也谈不上打破什么统一做法。**哪天想要自动依赖更新，从装 App 开始重来，不要复活那份配置**（那份还用着已废弃的 `matchPackagePatterns`）|
| ✅ | CTV-25 | **模板 README 是上游原文** | **2026-09-09 完成。条目原文说「换了要维护 8 份」——实测只有 2 份**：仅 `template-vue` 与 `template-vue-ts` 有 README（Vite 官方英文原文），另外 4 个（vanilla / vanilla-ts / lit / lit-ts）**一份都没有**。Hubery 选定「6 个模板都给一份、写模板名、静态不改写」，于是改写 2 份、新建 4 份，内容一律 `# <模板名>`。**`src/` 零改动**——README 刻意不进改写链路（`package.json` 的 `name` 与 `index.html` 的 `<title>` 会被改成包名，README 是留给用户的白纸）。`files` 用的是 `template-*/**` 通配，新文件自动进产物。**钉子两处**：`constants.test.ts` 断言六个目录都有 README 且内容恰好是 `# <模板名>`（期望值来自目录名、被断言的是文件内容，两者不同源，不是恒等式）；`scaffold.e2e.test.ts` 用「目录 my-app + 模板 vanilla」让两个来源互相区分，断言生成后仍是 `# vanilla`，钉住「不被改写」——**这条是补上的缺口**：新增 4 份 README 竟没打红任何既有 E2E，因为只有 vue-ts 有完整文件树断言（成本：小） |
| ✅ | CTV-32 | **changelogithub 要不要扩展收录的提交类型** | 默认只收 `feat` / `fix` / `perf`。实测：v1.1.0 的 7 个提交露出 2 条，v1.2.0 的 5 个提交也只露出 2 条——**CTV-15 那次 265 行的重构在 Release notes 里完全隐形**。加 `refactor` / `docs` 能让发布说明更完整，但也会把噪音放进去。**卡在哪**：这份 Release notes 的读者是谁？如果只面向使用者，`refactor` 本就不该露面；如果也当作变更档案，那就该扩。**2026-09-09 Hubery 选定「收 refactor / docs / build，不收 chore / test / ci」，已完成**：新增根目录 `changelogithub.config.ts`，只列这三类新增类型——**默认的 feat / fix / perf 是被合并而不是被替换**（实测 15.0.5 的 `resolveConfig`：带配置时 types 为 `feat, fix, perf, refactor, docs, build`，不带时只有前三个）。**刻意不放进 `package.json` 的 `changelogithub` 字段**（两种形态都实测生效），那份会随包发给使用者；独立文件也不在 `files` 里，`npm pack` 仍是 82 个文件、配置命中 0。不收 chore 的依据是实测：仓库 11 条 chore **全部**是 `chore: release vX.Y.Z`。**验证用真实 dry run 改前改后对照**：`--to v1.6.0` 那 4 个提交，改前只露 1 条 fix，改后多出 💅 Refactors（`0002f55`）与 📦 Build（`6476f0e`）两组、`chore: release v1.6.0` 仍不出现；`v1.1.0..v1.2.0` 区间从 2 条变 6 条，CTV-15 那次 265 行重构终于露面。**已知代价**：`d0dd9a0 docs: design the CTV-15 split` 这种内部设计文档提交现在也会进发布说明，缓解办法是以后这类提交改用 `chore(docs):` |
| ✅ | CTV-35 | **README 说 `custom-*` 只能「交互式选择 Vue 时」用，但 `-t` 直接传也能用** | `TEMPLATES` 由 `FRAMEWORKS` flatMap 派生，`custom-nuxt` / `custom-vitesse` 这些一直都是 `-t` 的合法值。CTV-30 把它们补进 `--help` 的「可用模板」之后，README 这句不准确变得更刺眼——用户从 help 看到名字，回 README 却被告知要交互式选。**做 CTV-30 时刻意没顺手改**（设计里说好了不扩范围），一句话的事，但属于另一个问题（成本：小） |
| ✅ | CTV-33 | **扫一遍「把模块级常量直接交给第三方库」的写法** | 2026-09-07 踩到：`mri` 会就地改写传入的配置对象（alias 值变数组、`alias.help` 变 `[]`、`boolean` 每次调用追加一轮且无上限），而 CTV-17 把模块级的 `ARGV_OPTIONS` 直接传了进去。已用 `structuredClone` 修掉。**同类风险**：仓库里还有没有别处把共享常量交给第三方库？`Object.freeze` 挡不住这类问题（CJS 库非严格模式下赋值静默失败，且浅冻结管不到嵌套对象——已实测）。**2026-09-09 已完成。审计结论：`src/` 里没有第二处。**第三方库只有 4 个（mri / cross-spawn / @clack/prompts / picocolors）；`FRAMEWORKS` 交给 clack 的两处（`index.ts:297`、`316`）都是 `.map()` 返回**全新对象字面量**，没把注册表节点本身交出去；`TEMPLATES` / `RENAME_FILES` 只流向自有函数，`planTemplateFiles` / `findVariantCommand` / `resolveArgTemplate` / `collectKnownFlags` 四个已逐个读过，全是纯读取（spread / filter / map 造新值）。**但扫出了一个防护缺口**：`ARGV_OPTIONS` 有钉子，`FRAMEWORKS` 与 `RENAME_FILES` 没有——实测往 `main()` 注入一行 `FRAMEWORKS[0].variants[0].name = …`，**239 条单测全绿、一条都没红**。根因是派生物挡不住：`TEMPLATES` 与 `HELP_MESSAGE` 都在模块加载时求值一次，之后再改 `FRAMEWORKS` 它们不跟着变，`constants.test.ts` 因此永远看不见。**已补钉子**（`tests/entry.test.ts` 的「调用 main() 不会改写模板注册表」，期望值全硬编码、不从 `FRAMEWORKS` 派生），**3 个变异 3 个被捕获**（改变体名 / 改框架名 / 改 `RENAME_FILES` 映射值）。**顺带验证了既有钉子确实有牙齿**：把 `structuredClone` 拿掉还原 CTV-17 的原始缺陷，`boolean` 数组从 4 项涨到 **191 项**，两条用例转红。单测 239 → 240，`src/` 零改动 |
| ✅ | CTV-34 | **`intro()` 的框线两头都没收好** | 2026-09-07 实测：`src/index.ts` 收尾处 `if (immediate) install(...)` 分支**根本没调 `outro()`**，`intro()` 打出的 `┌` 永远没有配对的 `└`；`else` 分支调了 `outro()`，却又在框子关掉之后打了一句 `prompts.log.success('程序结束')`，实测输出里 `◆ 程序结束` 落在 `└` 外面。CTV-02 当初补 `intro()` 正是为了修断掉的框线，这是同一个病没收完的另一半。**与 CTV-21 同处一段代码但刻意分开做**（2026-09-07 Hubery 决定「单开单做」）：CTV-21 是补缺失的信息，本条是修渲染，混在一起会让「装完依赖后到底该打什么」这个问题带上两种动机 |
| ✅ | CTV-36 | **非交互报错后没恢复光标** | 2026-09-08 做 CTV-31 时实测：非交互路径的输出里只有 `ESC[?25l`（隐藏光标）**没有配对的 `ESC[?25h`**，而正常成功路径两者成对。根因是 clack 的 prompt 从未 settle，它自己的清理逻辑一行都没跑，`cancel()` 也不负责恢复光标。**这是 CTV-31 之前就有的行为，不是新引入的回归**——只是那时进程直接死掉，现在会继续输出，问题才显形。影响面窄（这条路径按定义是非交互的，多数时候对面是 CI 日志而不是终端），但 stdin 中途被关掉的交互式会话会留下一个没有光标的终端。**与 CTV-34 同属「clack 渲染状态没收好」**，可以一起做；一行 `process.stdout.write('\u001B[?25h')` 就能修，但该不该由我们替 clack 收尾、还是给上游提 issue，归 Hubery 定 |
| ✅ | CTV-37 | **非法 `--package-name` 的报错打断了 clack 框线** | 2026-09-08 对 v1.5.0 发布产物实测：这条路径的 stdout 只有一个 `┌  create-todo-vue`（intro 开的框，永远没有 `└`），报错文本走 stderr 落在框外。**根因是位置**——该校验排在 `intro()` 之后，而它旁边的注释写着「与『未知参数』形态保持一致」，那句话**是错的**：未知参数校验发生在 `intro()` 之前，stdout 一个字都没有，两者并不同形（注释是 CTV-31 时我写的，已实测证伪）。退出码、报错内容、「什么都不创建」这三点都正确，纯属渲染问题。**修法**：显式传了 `--package-name` 时它的合法性不依赖 `targetDir`，可以把这一支提到 `intro()` 之前，注释那句话也就真了；或者改走 clack 输出。与 CTV-34 / CTV-36 同属「clack 框线/渲染状态没收好」，建议三条一起做 |
| ✅ | CTV-38 | **`--package-name` 同时改写了 `index.html` 的 `<title>`，README 没说** | 2026-09-08 实测：`--package-name my-pkg` 建出来的项目里 `<title>my-pkg</title>`。**这是既有耦合不是新行为**——`scaffoldTemplate()` 一直用同一个 `packageName` 同时写 `package.json` 的 `name` 和 `<title>`，CTV-31 只是第一次让用户能显式控制它。但 README 写的是「指定 `package.json` 的 `name`」，少说了一半。**待定的是修哪边**：补文档（承认耦合），还是拆成两个概念（那就是新功能，得再开条目）。**2026-09-09 Hubery 选定补文档，已完成**：README 改两处——参数说明那条补上 `<title>`；`custom-*` 那段「改写成你的项目名」统一成「改写成包名」（传了 `--package-name` 时 title 跟的是包名不是目录名，两处措辞不统一会指向两个东西）。**并补了一条 E2E 钉住这句话**：目录 `my-app` + `--package-name other-name`，断言 `<title>other-name</title>`。场景刻意让三个来源互相区分（目录名 / 包名 / 模板原文 `Vite + JS`）——`scaffold.e2e.test.ts:88` 那条 `<title>my-app</title>` 顶不上，它没传 `--package-name`，目录名与包名同源。**测试是在红的状态下写的**：先往 `scaffold.ts` 注入变异让行为变错，看见红再还原，不是「写完一次就绿」。**2 个变异 2 个被捕获**（title 改跟目录名走 → 红；`case index-html` 退化成 copy → 红），每次还原后 `shasum` 与基线一致。零行为变更，`dist/index.js` 用 stash 前后各构建一次实测**逐字节相同** |
| ✅ | CTV-39 | **安装失败时收尾兜底被跳过** | 2026-09-08 做 CTV-34 时发现并**刻意没修**。`utils.ts` 私有的 `run()` 在包管理器返回非零时直接 `process.exit(status)`，于是 `runCli()` 的 `finally` 一行都不跑——框留着开、光标不恢复。这是 CTV-34 那套「框线有主人」保证里唯一的漏网之鱼，`src/index.ts` 内部已经没有 `process.exit` 了。**为什么没顺手修**：它属于「安装失败的错误处理」而不是渲染，改法要动 `utils` 的契约（`run()` 改成抛异常），而且得想清楚怎么保住包管理器的原始退出码——现在 `npm install` 失败退几，CLI 就退几，改成抛异常会一律变成 1，那是**行为变更**不是渲染修复。**做之前先定**：原始退出码要不要保？要的话得把它一路带到 `runCli`。E2E 已经有 `stubPackageManager` 能力，让 stub 退非零就能覆盖 |
| ❌ | CTV-40 | ~~发版分支护栏：不在 main 上就拒绝发版~~ | **2026-09-09 放弃**：Hubery 决定**不加这个脚本**，发版分支由他自己控制。（此前 09-09 已把它从「该赶紧做」下调为「可选的保险」，本次直接结掉。）**背景留档**：v1.5.1 与 v1.6.0 连续两次误在 `dev` 上发版，`bumpp` 默认 `--commit --tag --push` 把发版提交和 tag 推到当前分支，于是 tag 一度不在默认分支、`main` 落后（两次都由 Claude 事后快进补救）；v1.6.1 是第一次走对。**放弃的代价**：这个坑没有机械护栏，仍然靠人记发版清单第 1 条「先合回 main」。**哪天重开，两条实测结论直接可用**：① **别把脚本叫 `prerelease`**——实测（项目 pin 的 pnpm@11.25.0 与 npm 11.6.2 行为一致）`pnpm run release` 会**自动触发** `prerelease` 与 `postrelease`，所以原方案写的 `"release": "prerelease && build:prod && ..."` 会让护栏跑两次；而若改为不显式串、只靠钩子，护栏就依赖 `enable-pre-post-scripts` 这类配置，一旦被关掉会**静默失效**——正是本条目当初警惕 `preversion` 时怕的那个形态。该起一个不撞 lifecycle 的名字（如 `guard:release`）并显式串。② **只判「当前分支是不是 main」不够**——真正要保证的是 tag 落在默认分支的**最新**提交上，`main` 落后于 `origin/main` 时照样出问题。原条目把「校验与 origin/main 同步」列为「顺带」，实际它可能才是主要价值 |
| ❌ | CTV-28 | ~~TypeScript 升到 7.x~~ | **2026-09-10 Hubery 决定放弃：短期内不升 TS。** 原委留档以免重查：最新是 7.0.2（Go 重写版），但 `@antfu/eslint-config@9` 依赖的 `typescript-eslint@8.69.0` peer 范围是 `>=4.8.4 <6.1.0`，装 7 会直接 unmet peer（**已实测确认**），故压在 6.0.3——TS 版本不是我们能单独决定的，它由 eslint 工具链的 peer 范围锁着。**哪天真要升，从确认 typescript-eslint 是否已支持 TS 7 重新开始**，别直接照搬这里的版本号（它们会过期）；届时还要留意 `tsc` 换成 `tsgo` 后的行为差异。注意 `engines` 声明的是**发布产物对使用者**的要求，与开发所需的 Node/TS 版本是两回事，升 devDependencies 不该顺手抬它 |

